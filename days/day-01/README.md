# Day 01 — Example

This folder contains a small example for day 01.

Guidelines:
- Keep functions small and testable.
- Add tests in `test_*.py`.
- Use descriptive names.

Files:
- `example.py` — simple function(s) to implement
- `test_example.py` — tests for the functions